const String HOST = 'localhost';
const int PORT = 8080;              
const SECRET_KEY = "YOUR_SECRET_KEY";       // Secret key is used for generating JWTs.
const AUTH_ENABLE = true;          // If this is true, then only we require JWTs for accessing APIs.